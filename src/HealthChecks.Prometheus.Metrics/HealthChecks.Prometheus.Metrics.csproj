<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFrameworks>$(DefaultNetCoreTargetFrameworks)</TargetFrameworks>
    <PackageTags>$(PackageTags);Prometheus;Prometheus Exporter;Metrics</PackageTags>
    <Description>HealthChecks.Publisher.Prometheus is a health check prometheus metrics exporter.</Description>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Remove="Microsoft.Extensions.Diagnostics.HealthChecks" />
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
    <PackageReference Include="prometheus-net" />
  </ItemGroup>

</Project>

