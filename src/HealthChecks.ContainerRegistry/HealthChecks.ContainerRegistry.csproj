<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFrameworks>$(ExtendedLibraryTargetFrameworks)</TargetFrameworks>
    <PackageTags>$(PackageTags);ContainerRegistry;OCI;Docker;Registry;Distribution</PackageTags>
    <Description>HealthChecks.ContainerRegistry is the health check package for OCI and Docker Registry HTTP API v2 endpoints.</Description>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Http" />
  </ItemGroup>

</Project>
