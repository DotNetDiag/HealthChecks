<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFrameworks>$(ExtendedLibraryTargetFrameworks)</TargetFrameworks>
    <PackageTags>$(PackageTags);CockroachDb;CockroachDB;SQL;Database;PostgreSQL</PackageTags>
    <Description>HealthChecks.CockroachDb is the health check package for CockroachDB SQL connectivity and node readiness endpoints.</Description>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Http" />
    <PackageReference Include="Npgsql" />
  </ItemGroup>

</Project>
