name: HealthChecks OpenSearch CI

permissions:
  contents: read
  packages: write


on:
  workflow_dispatch:
  push:
    branches: [ master ]
    paths:
      - src/HealthChecks.OpenSearch/**
      - test/HealthChecks.OpenSearch.Tests/**
      - test/_SHARED/**
      - .github/workflows/healthchecks_opensearch_ci.yml
      - .github/workflows/reusable_ci_workflow.yml
      - Directory.Build.props
      - Directory.Build.targets
      - Directory.Packages.props
    tags:
      - v*

  pull_request:
    branches: [ master ]
    paths:
      - src/HealthChecks.OpenSearch/**
      - test/HealthChecks.OpenSearch.Tests/**
      - test/_SHARED/**
      - .github/workflows/healthchecks_opensearch_ci.yml
      - .github/workflows/reusable_ci_workflow.yml
      - Directory.Build.props
      - Directory.Build.targets
      - Directory.Packages.props

jobs:
  build:
    uses: ./.github/workflows/reusable_ci_workflow.yml
    secrets: inherit
    with:
      PROJECT_PATH: ./src/HealthChecks.OpenSearch/HealthChecks.OpenSearch.csproj
      TEST_PROJECT_PATH: ./test/HealthChecks.OpenSearch.Tests/HealthChecks.OpenSearch.Tests.csproj
      CODECOV_FLAGS: OpenSearch
