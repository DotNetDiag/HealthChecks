name: HealthChecks CockroachDB CI

permissions:
  contents: read
  packages: write


on:
  workflow_dispatch:
  push:
    branches: [ master ]
    paths:
      - src/HealthChecks.CockroachDb/**
      - test/HealthChecks.CockroachDb.Tests/**
      - test/_SHARED/**
      - .github/workflows/healthchecks_cockroachdb_ci.yml
      - .github/workflows/reusable_ci_workflow.yml
      - Directory.Build.props
      - Directory.Build.targets
      - Directory.Packages.props
    tags:
      - v*

  pull_request:
    branches: [ master ]
    paths:
      - src/HealthChecks.CockroachDb/**
      - test/HealthChecks.CockroachDb.Tests/**
      - test/_SHARED/**
      - .github/workflows/healthchecks_cockroachdb_ci.yml
      - .github/workflows/reusable_ci_workflow.yml
      - Directory.Build.props
      - Directory.Build.targets
      - Directory.Packages.props

jobs:
  build:
    uses: ./.github/workflows/reusable_ci_workflow.yml
    secrets: inherit
    with:
      PROJECT_PATH: ./src/HealthChecks.CockroachDb/HealthChecks.CockroachDb.csproj
      TEST_PROJECT_PATH: ./test/HealthChecks.CockroachDb.Tests/HealthChecks.CockroachDb.Tests.csproj
      CODECOV_FLAGS: CockroachDb
