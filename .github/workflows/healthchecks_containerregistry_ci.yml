name: HealthChecks ContainerRegistry CI

permissions:
  contents: read
  packages: write


on:
  workflow_dispatch:
  push:
    branches: [ master ]
    paths:
      - src/HealthChecks.ContainerRegistry/**
      - test/HealthChecks.ContainerRegistry.Tests/**
      - test/_SHARED/**
      - .github/workflows/healthchecks_containerregistry_ci.yml
      - .github/workflows/reusable_ci_workflow.yml
      - Directory.Build.props
      - Directory.Build.targets
      - Directory.Packages.props
    tags:
      - v*

  pull_request:
    branches: [ master ]
    paths:
      - src/HealthChecks.ContainerRegistry/**
      - test/HealthChecks.ContainerRegistry.Tests/**
      - test/_SHARED/**
      - .github/workflows/healthchecks_containerregistry_ci.yml
      - .github/workflows/reusable_ci_workflow.yml
      - Directory.Build.props
      - Directory.Build.targets
      - Directory.Packages.props

jobs:
  build:
    uses: ./.github/workflows/reusable_ci_workflow.yml
    secrets: inherit
    with:
      PROJECT_PATH: ./src/HealthChecks.ContainerRegistry/HealthChecks.ContainerRegistry.csproj
      TEST_PROJECT_PATH: ./test/HealthChecks.ContainerRegistry.Tests/HealthChecks.ContainerRegistry.Tests.csproj
      CODECOV_FLAGS: ContainerRegistry
