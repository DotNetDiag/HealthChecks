name: Legacy HealthChecks Publisher.ApplicationInsights CD

on:
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable_cd_workflow.yml
    secrets: inherit
    with:
      BUILD_CONFIG: Release
      PROJECT_PATH: ./src/HealthChecks.Publisher.ApplicationInsights/HealthChecks.Publisher.ApplicationInsights.csproj
      PACKAGE_NAME: NetDiag.HealthChecks.Publisher.ApplicationInsights
