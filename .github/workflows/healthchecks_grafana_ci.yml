name: HealthChecks Grafana CI

permissions:
  contents: read
  packages: write


on:
  workflow_dispatch:
  push:
    branches: [ master ]
    paths:
      - src/HealthChecks.Grafana/**
      - test/HealthChecks.Grafana.Tests/**
      - test/_SHARED/**
      - .github/workflows/healthchecks_grafana_ci.yml
      - .github/workflows/reusable_ci_workflow.yml
      - Directory.Build.props
      - Directory.Build.targets
      - Directory.Packages.props
    tags:
      - v*

  pull_request:
    branches: [ master ]
    paths:
      - src/HealthChecks.Grafana/**
      - test/HealthChecks.Grafana.Tests/**
      - test/_SHARED/**
      - .github/workflows/healthchecks_grafana_ci.yml
      - .github/workflows/reusable_ci_workflow.yml
      - Directory.Build.props
      - Directory.Build.targets
      - Directory.Packages.props

jobs:
  build:
    uses: ./.github/workflows/reusable_ci_workflow.yml
    secrets: inherit
    with:
      PROJECT_PATH: ./src/HealthChecks.Grafana/HealthChecks.Grafana.csproj
      TEST_PROJECT_PATH: ./test/HealthChecks.Grafana.Tests/HealthChecks.Grafana.Tests.csproj
      CODECOV_FLAGS: Grafana
