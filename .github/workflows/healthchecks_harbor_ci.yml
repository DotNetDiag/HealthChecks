name: HealthChecks Harbor CI

permissions:
  contents: read
  packages: write


on:
  workflow_dispatch:
  push:
    branches: [ master ]
    paths:
      - src/HealthChecks.Harbor/**
      - test/HealthChecks.Harbor.Tests/**
      - test/_SHARED/**
      - .github/workflows/healthchecks_harbor_ci.yml
      - .github/workflows/reusable_ci_workflow.yml
      - Directory.Build.props
      - Directory.Build.targets
      - Directory.Packages.props
    tags:
      - v*

  pull_request:
    branches: [ master ]
    paths:
      - src/HealthChecks.Harbor/**
      - test/HealthChecks.Harbor.Tests/**
      - test/_SHARED/**
      - .github/workflows/healthchecks_harbor_ci.yml
      - .github/workflows/reusable_ci_workflow.yml
      - Directory.Build.props
      - Directory.Build.targets
      - Directory.Packages.props

jobs:
  build:
    uses: ./.github/workflows/reusable_ci_workflow.yml
    secrets: inherit
    with:
      PROJECT_PATH: ./src/HealthChecks.Harbor/HealthChecks.Harbor.csproj
      TEST_PROJECT_PATH: ./test/HealthChecks.Harbor.Tests/HealthChecks.Harbor.Tests.csproj
      CODECOV_FLAGS: Harbor
