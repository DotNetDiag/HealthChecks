name: HealthChecks IoTDB CI

permissions:
  contents: read
  packages: write


on:
  workflow_dispatch:
  push:
    branches: [ master ]
    paths:
      - src/HealthChecks.IoTDB/**
      - test/HealthChecks.IoTDB.Tests/**
      - test/_SHARED/**
      - .github/workflows/healthchecks_iotdb_ci.yml
      - Directory.Build.props
      - Directory.Build.targets
      - Directory.Packages.props
    tags:
      - v*

  pull_request:
    branches: [ master ]
    paths:
      - src/HealthChecks.IoTDB/**
      - test/HealthChecks.IoTDB.Tests/**
      - test/_SHARED/**
      - .github/workflows/healthchecks_iotdb_ci.yml
      - Directory.Build.props
      - Directory.Build.targets
      - Directory.Packages.props

jobs:
  build:
    uses: ./.github/workflows/reusable_ci_workflow.yml
    secrets: inherit
    with:
      PROJECT_PATH: ./src/HealthChecks.IoTDB/HealthChecks.IoTDB.csproj
      TEST_PROJECT_PATH: ./test/HealthChecks.IoTDB.Tests/HealthChecks.IoTDB.Tests.csproj
      CODECOV_FLAGS: IoTDB
